# Electron downloads its binary in a postinstall script. The project root sets
# ignore-scripts=true, which would skip that and leave npm install "succeeding"
# with no Electron to run, failing later as "Error: Electron uninstall".
ignore-scripts=false
